node_modules/
*.sqlite
*.sqlite-wal
*.sqlite-shm
dist/
# Allow the pre-built frontend dist to be committed (clearco-ui is a local
# sibling repo not available in Netlify CI, so we commit the built output)
!poc/frontend/dist/
!poc/frontend/dist/**
.env
.DS_Store
screenshots/
capture.js
capture-product.js
